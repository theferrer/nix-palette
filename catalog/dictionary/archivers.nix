{ pkgs }:
{
  p7zip = {
    package = pkgs.p7zip;
  };
  unrar = {
    package = pkgs.unrar;
  };
  unzip = {
    package = pkgs.unzip;
  };
  xarchiver = {
    package = pkgs.xarchiver;
  };
  zip = {
    package = pkgs.zip;
  };
  arj = {
    package = pkgs.arj;
  };
  lhasa = {
    package = pkgs.lhasa;
  };
  cabextract = {
    package = pkgs.cabextract;
  };
}
