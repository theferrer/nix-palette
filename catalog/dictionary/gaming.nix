{ pkgs }:
{
  heroic = {
    package = pkgs.heroic;
  };

  bottles = {
    package = pkgs.bottles;
  };

  gamescope = {
    package = pkgs.gamescope;
  };

  goverlay = {
    package = pkgs.goverlay;
  };

  wine = {
    package = pkgs.wine;
  };

  protontricks = {
    package = pkgs.protontricks;
  };

  r2modman = {
    package = pkgs.r2modman;
  };

  minecraft = {
    package = pkgs.prismlauncher;
  };

}
